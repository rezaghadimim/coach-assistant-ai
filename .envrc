# Auto-activate the project venv (Python 3.12) when you cd into this directory.
watch_file .python-version
watch_file pyproject.toml

if [ ! -d .venv ]; then
  uv venv
  uv sync
fi

source .venv/bin/activate
