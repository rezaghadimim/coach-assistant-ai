# Dependencies
node_modules/
.pnp
.pnp.js

# Environment
.env
.env.local
.env.production

# Build
dist/
build/
.next/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Python (for fine-tuning scripts)
__pycache__/
*.pyc
.venv/
venv/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Models (large files)
/models/
*.bin
*.gguf

# Ollama
ollama_data/

# Database
*.db
*.sqlite

# Logs
logs/
*.log

# Vector store local data
chroma_data/

# RAG reranker model cache (downloaded on first use, ~1 GB)
data/rerank_cache/

# RAG embedding index cache (regenerated on startup/ingest)
data/rag_index_cache.json

# Fine-tuning data and artifacts (local only — not for git)
data/external/
data/training/
artifacts/lora/
artifacts/eval/
training_data.jsonl

# Graphify (codebase knowledge graph) — local-only / regenerable bits.
# Commit graphify-out/graph.json + GRAPH_REPORT.md if you want a shared map.
graphify-out/cost.json
graphify-out/cache/
